import {StyleSheet, Text, View} from 'react-native';
import React from 'react';

type Props = {};

const AlertMonfirm = (props: Props) => {
  return (
    <View>
      <Text>AlertMonfirm</Text>
    </View>
  );
};

export default AlertMonfirm;

const styles = StyleSheet.create({});
